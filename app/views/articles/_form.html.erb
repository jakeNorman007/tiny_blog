<%# edit form %>
<%# different from the new article form because I am inserting the saved article info into the form fields. Note to self, when you're doing this do it right away when this .erb file is created %>
<%= form_with(model: @article, local: true) do |f|%>
  <p>
    <%= f.label :title %>
    <br/>
    <%= f.text_field :title %>
  </p>
  <p>
    <%= f.label :description %>
    <br/>
    <%= f.text_area :description %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>