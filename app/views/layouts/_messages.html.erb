<% flash.each do |name, msg| %>
  <% if notice %>
    <input type="text" placeholder="<%= msg %>" aria-invalid="false">
  <% else %>
    <input type="text" placeholder="<%= msg %>" aria-invalid="true">
  <% end %>
<% end %>
